name: 🚀 Zip Deploy Azure Python Function

on:
  push:
    branches:
      - main

jobs:
  zip-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 📦 Checkout repository
      uses: actions/checkout@v3

    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📁 Install dependencies and prepare build folder
      run: |
        mkdir -p build/.python_packages/lib/site-packages
        pip install -r requirements.txt -t build/.python_packages/lib/site-packages

        cp requirements.txt host.json build/
        cp -r \
          IntradayAlert \
          OutcomeTracker \
          Watchlist \
          rss_listener \
          db_writer \
          logger \
          keyvault_loader \
          discord_poster \
          entry_target \
          finnhub_api \
          mcp_score \
          nlp_processor \
          build/

    - name: 🧪 Verify build contents before zipping
      run: |
        echo "::group::Build Folder Structure"
        tree build/
        echo "::endgroup::"

    - name: 📦 Create deployment package
      run: |
        cd build
        zip -r ../function.zip . -x "*.git*" -x "*__pycache__*"

    - name: 🚀 Deploy to Azure Function App
      uses: Azure/functions-action@v1
      with:
        app-name: quant-bot-app
        package: function.zip
        publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
